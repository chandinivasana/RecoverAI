# Blade ships react-native peers that are irrelevant for web consumers; npm's
# strict peer resolution would otherwise try (and fail) to install the RN tree.
# This mirrors how Razorpay web apps consume @razorpay/blade.
legacy-peer-deps=true
